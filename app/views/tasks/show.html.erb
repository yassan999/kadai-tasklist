<h1><%= @user.name %>のタスク詳細ページ</h1>

<p><%= @task.content %> > <%= @task.status %></p>

<%= link_to '一覧', root_path, class: 'btn btn-default btn-xs' %>
<%= link_to '変更', edit_task_path(@task), class: 'btn btn-primary btn-xs' %>
<%= link_to "Delete", @task, method: :delete, data: { confirm: "You sure?" }, class: 'btn btn-danger btn-xs' %>
